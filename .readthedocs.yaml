version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.10"

sphinx:
  configuration: docs/conf.py # Chemin vers votre conf.py
  fail_on_warning: true # Optionnel : échoue si des warnings

python:
  install:
    - method: pip
      path: . # Installe votre projet (si nécessaire)
    - requirements: docs/requirements.txt # Dépendances
