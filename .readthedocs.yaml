version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.10"

sphinx:
  configuration: docs/conf.py  # ← Assure-toi que ce chemin est correct !

python:
  install:
    - requirements: docs/requirements.txt
